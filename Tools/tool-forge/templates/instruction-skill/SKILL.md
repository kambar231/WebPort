---
name: __TOOL_NAME__
description: "TODO — what it does + 'Use whenever the user…' with exact phrases + concrete task nouns + 'Do NOT use for…'. 100–200 words, third person, no angle brackets."
---

# __TOOL_NAME__

TODO: one-line premise — the single idea this skill encodes.

## Process

TODO: the workflow as numbered imperative steps. For each step, say *why* it matters where
a rule could otherwise be ignored. One default path + escape hatches, not a menu.

## Rules

TODO: the judgment calls, as terse bullets — bolded lead + the reason. Consistent
terminology (one term per concept). No general programming education; Claude knows the
domain already.

## Example

TODO: one worked example — realistic input → the exact expected output. Concrete beats
abstract; a second example only if it covers a genuinely different case.

## When things go wrong

TODO: the 2–3 most likely failure modes and what to do instead.
